# 베이스 이미지 선택 (Node.js 18 버전 사용)
FROM node:18-alpine

# 작업 디렉토리 설정
WORKDIR /usr/src/app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 종속성 설치
# npm 사용 시
RUN npm install

# 소스 코드 복사 (아래 docker-compose.yml에서 볼륨 마운트 예정)
# COPY . .

# 개발 서버 포트 노출
EXPOSE 3000

# 개발 서버 실행
CMD ["npm", "run", "dev"]